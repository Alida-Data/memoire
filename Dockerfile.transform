FROM python:3.10-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn pandas scikit-learn mlflow

COPY transform.py /app/transform.py

CMD ["uvicorn", "transform:app", "--host", "0.0.0.0", "--port", "8000"]